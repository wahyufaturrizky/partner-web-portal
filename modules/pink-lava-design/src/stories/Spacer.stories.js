import React from 'react';

import { Spacer as SpacerComponent } from '../components/Spacer';

export default {
  title: 'Pink Lava/Spacer',
  component: SpacerComponent,
};

const Template = (args) => <SpacerComponent {...args} />;

export const Spacer = Template.bind({});