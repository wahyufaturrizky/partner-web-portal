import React from 'react';

import { Login as LoginComponent } from '../../modules/Login';

export default {
  title: 'Pink Lava/Login',
  component: LoginComponent,
};

const Template = (args) => <LoginComponent />;

export const Login = Template.bind({});